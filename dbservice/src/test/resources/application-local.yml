spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: jdbc:h2:mem:SPECBOOK_OWNER;DB_CLOSE_DELAY=-1
    username: sa
    password: sa